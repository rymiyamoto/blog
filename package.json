{
  "name": "vitepress-project",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tsx scripts/generatePages.ts",
    "dev": "pnpm install && pnpm generate && vitepress dev docs --host",
    "build": "pnpm generate && vitepress build docs"
  },
  "devDependencies": {
    "@types/node": "24.0.10",
    "tsx": "4.20.3",
    "vitepress": "1.6.3",
    "vue": "3.5.17"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@algolia/client-search"
      ]
    },
    "ignoredBuiltDependencies": [
      "esbuild",
      "vue-demi"
    ]
  },
  "dependencies": {
    "generate": "0.14.0",
    "gray-matter": "4.0.3",
    "pnpm": "10.12.4"
  }
}
